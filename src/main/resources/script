CREATE KEYSPACE IF NOT EXISTS pagos_socios WITH replication = {'class':'SimpleStrategy', 'replication_factor':1};

CREATE TABLE IF NOT EXISTS  pagos_socios.Employee (
    id varchar,
    firstName varchar,
    lastName varchar,
    email varchar,
    primary key((id))
);

